apiVersion: v1
kind: Service
metadata:
  name: vllm-slm
  namespace: vllm-slm
  labels:
    app.kubernetes.io/name: vllm-slm
    app.kubernetes.io/part-of: hybrid-inference
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: vllm-slm
  ports:
  - name: api
    port: 8000
    targetPort: api
    nodePort: 30500
    protocol: TCP
